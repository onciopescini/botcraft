name: weekly-tournament
on:
  schedule:
    - cron: "0 19:00 * * 5"
  workflow_dispatch:
jobs:
  tourney:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - run: pip install -r requirements.txt
      - run: python scripts/weekly.py --title weekly-auto
      - uses: actions/upload-artifact@v4
        with:
          name: weekly-report
          path: matches/tourney-*/report.json
